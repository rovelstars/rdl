import { useTranslation, withTranslation } from "react-i18next";

export default function Navbar() {
    const { t, i18n } = useTranslation();
    return (
        // make a circle image avatar with tailwind
        <div className="">
            
        </div>
    );
  }